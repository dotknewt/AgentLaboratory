# AgentLaboratory direct dependencies.
# Floors are the first releases with Python 3.13 wheels; pip resolves the
# newest compatible versions, which also ship Python 3.14 wheels (best-effort).
# Tested on Python 3.12 and 3.13.
#
# NOTE: tensorflow is intentionally NOT listed. common_imports.py imports it
# optionally; install it manually if your generated experiments need it.

# LLM provider APIs
openai>=1.55
anthropic>=0.39
google-genai>=1.0

# Literature / paper tooling
arxiv>=2.1
semanticscholar>=0.8
pypdf>=5.1

# Hugging Face / deep learning
torch>=2.6
transformers>=4.46
tokenizers>=0.21
accelerate>=1.1
datasets>=3.1
diffusers>=0.31
huggingface_hub>=0.26
sentence-transformers>=3.2
tiktoken>=0.8

# Scientific stack
numpy>=2.1
scipy>=1.14.1
pandas>=2.2.3
scikit-learn>=1.5.2
statsmodels>=0.14.4
h5py>=3.12
matplotlib>=3.9.2
seaborn>=0.13
plotly>=5.24
pillow>=11.0
imageio>=2.36
scikit-image>=0.25

# NLP
nltk>=3.9
spacy>=3.8.7
sacremoses>=0.1

# AgentRxiv web app
flask>=3.0
flask-sqlalchemy>=3.1
werkzeug>=3.1

# Utilities
aiohttp>=3.11
psutil>=6.1
pydantic>=2.10
PyYAML>=6.0
requests>=2.32
tqdm>=4.67
